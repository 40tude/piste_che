Read ARCHITECTURE.md
Update (do not rewrite) ARCHITECTURE.md to reflect the current implementation:
- module structure
- data flow (request → routing → response)
- key design decisions and why they were made
- what is intentionally out of scope for now
- Where to find files and documentation
- Run `git rev-parse --short HEAD` to get the current commit hash and update the Version line.
git commit -m "docs: update ARCHITECTURE"